# 1. Pin versions to avoid PyTorch 2.4 / NumPy 2.x conflicts (sentence-transformers + transformers)
numpy<2
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.4
sentence-transformers

# 2. RAG and Video Processing
youtube-transcript-api
groq
chromadb
pymupdf
python-docx

# 3. UI and Environment
gradio
python-dotenv>=1.0.0
pydantic>=2.5.0,<2.11
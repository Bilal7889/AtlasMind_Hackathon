# 1. Force CPU-only versions to stay under the 4GB limit
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.1+cpu
sentence-transformers

# 2. Core AI and RAG logic
yt-dlp
groq
chromadb
pymupdf
python-docx

# 3. Web Framework and UI
gradio
fastapi>=0.115.2
uvicorn>=0.14.0
python-dotenv>=1.0.0
cors>=1.0.1
httpx>=0.25.2

# 4. Database and Utilities (Only keep if you are using a real SQL database)
sqlalchemy>=2.0.23
psycopg2-binary>=2.9.9
alembic>=1.13.1
pydantic>=2.5.0,<2.11
pydantic-settings>=2.1.0
audioop-lts  # Required for Python 3.13 compatibility
